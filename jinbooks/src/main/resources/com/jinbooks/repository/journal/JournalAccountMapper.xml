<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinbooks.repository.journal.JournalAccountMapper">

	<select id="pageList"
		parameterType="com.jinbooks.dto.journal.JournalAccountPageDto"
		resultType="com.jinbooks.domain.journal.JournalAccount">
		select jja.*
		from journal_account jja

		where jja.deleted = 'n'
			and jja.book_id = #{dto.bookId}
		<if test="dto.accName != null and dto.accName != ''">
			and jja.acc_name like CONCAT('%', #{dto.accName}, '%')
		</if>
		order by jja.created_date desc
	</select>
</mapper>
